#pragma once
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

// 宏定义
#define MAX_NAME 20
#define MAX_PWD 20
#define MAX_ID 20

// 日期结构体
typedef struct {
    int year;
    int month;
    int day;
} Date;